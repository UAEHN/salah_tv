<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- TV: No touchscreen required -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>
    <!-- leanback required=false: app appears in both phone and TV Play Store sections -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false"/>
    <!-- Internet for Quran streaming -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Qibla: GPS for bearing calculation -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Keep screen on during Adhan -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Prayer time notifications (mobile) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- SCHEDULE_EXACT_ALARM: covers API 31-32; user can revoke on API 31+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- USE_EXACT_ALARM: API 33+, auto-granted for alarm/reminder apps, not revocable.
         Prayer apps qualify; replaces SCHEDULE_EXACT_ALARM as primary on API 33+. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Required to re-schedule alarms after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Required to request battery optimization exemption via system dialog -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Custom adhan import via SAF file picker (Android 13+). Below Android 13
         the SAF picker doesn't require any permission. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <!-- MediaStore insert of notification sounds on Android ≤ 9 requires
         WRITE_EXTERNAL_STORAGE. Android 10+ uses scoped storage instead. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <!-- Explicitly remove AD_ID permission injected by Firebase/Google SDKs -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>

    <application
        android:label="غسق"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/tv_banner"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- TV Launcher (Leanback) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <!-- Phone/debug launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Disable AD_ID collection: app has no ads, no ad targeting needed -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <!-- Home-screen widget (mobile only): rendered by AppWidgetManager.
             Native side computes "next prayer" and "remaining" from the raw
             timestamps written by Flutter via the home_widget plugin. -->
        <receiver
            android:name=".PrayerWidgetProvider"
            android:exported="true"
            android:label="@string/prayer_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info"/>
        </receiver>


        <!-- Re-arms the widget's per-minute alarm after device reboot. -->
        <receiver
            android:name=".widget.WidgetBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- Native notification engine: receives AlarmManager broadcasts and
             posts the notification on the system's behalf. exported=false
             keeps third-party apps from triggering it. -->
        <receiver
            android:name="com.ghasaq.app.notifications.receiver.AlarmReceiver"
            android:exported="false"/>

        <!-- Re-arms every persisted alarm after reboot / package replace. -->
        <receiver
            android:name="com.ghasaq.app.notifications.receiver.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="market"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
